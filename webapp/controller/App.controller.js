sap.ui.define([
  "sap/ui/core/mvc/Controller"
], (BaseController) => {
  "use strict";

  return BaseController.extend("com.bootcamp.sapui5.freestyle.controller.App", {
      /**
       * Método de inicialización del controlador que se ejecuta cuando la vista es instanciada.
       * @description Se llama automáticamente cuando el framework crea el controlador.
       * @public
       */
      onInit() {
      }
  });
});